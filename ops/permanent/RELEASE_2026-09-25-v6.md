# Durable release 2026-09-25 (v6) — main 8613d0c40db2

> Railway resource IDs are kept out of this public repository. Read them from the Railway dashboard.

Deployed on 2026-09-25 to `studio-web-permanent` (project
`mml-tools-studio-permanent`, environment `production`). It releases merged main
`8613d0c40db2`: the analysis Worker's boot recovery (#93) and the tightened G10
(#95). The Published Canonical is unchanged from v3–v5. This record was written
after the fact, from the deployment's startup log, when v7 was prepared.

## Identity

| Field | Value |
| --- | --- |
| Release/tag | [studio-v1-durable-8613d0c40db2](https://github.com/a91453/mml-tools/releases/tag/studio-v1-durable-8613d0c40db2) |
| Source SHA (merged main) | `8613d0c40db207a71a9b3d1c6cb40e6aae4728c3` |
| buildId | `9d0991bd36f076ff5d01724acd2963aa37f874d64b4c849452af7eddbddda4db` |
| cacheId | `824c9d2d29542b9f22b9a8c593e04f12efd22f3100b7caab84c778ab368d1b3c` |
| Asset count | 156, as in v5 |
| Runtime ZIP | `26f8fd2d92e15825eff01efcdb2c23c1bb1b842b9b87c44d6c5c36f1c9106c65`, 4,236,940 bytes |
| Trust ZIP | `28bdb56bf792105fe310737d6bb3e93c971fe3c46e64d9d40a45294a147e5a5b`, 18,984 bytes (the same bytes as v4 and v5) |
| Rendered function file | `b6ea935cfa5fd8bc…`, 41,250 bytes (embedded self-hash `62270972a020…`) |
| Canonical | `2026-09-23-v3` / `PUBLISHED` / `2026-09-23-v3-manifest1` / `ff1a9df054f5ca1ae42571067fc95feb274755ef`, `machine_delivery_schema: mabinogi-mobile-mml-studio/machine-delivery@2` |
| Manifest commit | `44f3f0082cf5c30328edf1c251398b844488ad0a` |

## How it was produced

1. **Pin.** Commit `793cb66` (merged in #96): the buildId was computed twice from `8613d0c40db2` (local dependencies and a fresh `npm install --ignore-scripts --package-lock=false`), `verify-studio-artifact.mjs` passed, and packaging gave byte-identical ZIPs and lock.
2. **Build and publish.** [Release run 36105957631](https://github.com/a91453/mml-tools/actions/runs/36105957631) was dispatched while `origin/main` was exactly the source SHA.
3. **Mirror to the bucket.** [Mirror run 36105988893](https://github.com/a91453/mml-tools/actions/runs/36105988893).
4. **Local proof under Bun**, as for v5: offline start from a seeded cache through `SERVER_STARTED`, and `RENDERED_SOURCE_MISMATCH` for a one-character tamper.
5. **Deploy.** Staged by the agent and committed by the owner from the Dashboard on 2026-09-25 10:33 UTC. The deployment reached `SUCCESS`; the v5 deployment became `REMOVED`.

## Startup log, in order

`RENDERED_SOURCE_VERIFIED` (`62270972a020…`) → `TRUSTED_SOURCE_VERIFIED`
(trust ZIP `28bdb56bf792…`, source `8613d0c40db2`) → `DURABLE_DOWNLOAD_STARTED`
→ dependency discovery `{}` ("No dependencies found, skipping bun install") →
`DURABLE_ZIP_VERIFIED` (4,236,940 bytes) → `ARTIFACT_VERIFIED` (156 assets,
Canonical as above) → `ARTIFACT_ATOMICALLY_COMMITTED`
(`/studio-cache/durable-v1/9d0991bd…`) → `READY_MARKER_WRITTEN_AFTER_VERIFICATION`
→ `SERVER_STARTED` (`bootstrap: durable-source`).

## Not verified from here

As for v5: `verify_deployed.py` was not run against the permanent URL (the
session container cannot reach `*.up.railway.app`), and a real-browser check by
the owner is still open.

## Rollback

- The v5 cache `durable-v1/3675ac65…` stays on the volume, so rollback starts from cache.
- **R1:** render the function from the ops files at main `8613d0c40db2` (the v5 lock; `build_function.py`) and replace the function source with it. Expect `VALIDATED_CACHE_STARTUP` with `refetch: false`.
- **Alternatively:** roll back to the v5 deployment in the Dashboard's deployment history.
- Never delete cache namespaces, bucket objects, Releases or tags.
