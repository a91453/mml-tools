# Durable release 2026-09-25 (v7) — main 5dbf42a51c83

> Railway resource IDs are kept out of this public repository. Read them from the Railway dashboard.

Deployed on 2026-09-25 to `studio-web-permanent` (project
`mml-tools-studio-permanent`, environment `production`). It releases merged main
`5dbf42a51c83`, which carries #97–#105: listening recovery, run and finalize
guidance, imported decisions held pending, readiness only when the emitter
writes the Final, the sound-bank race fixes, ops and test hygiene, and pasting
raw MML straight into the listening panel. The Published Canonical is unchanged
from v3–v6.

## Identity

| Field | Value |
| --- | --- |
| Release/tag | [studio-v1-durable-5dbf42a51c83](https://github.com/a91453/mml-tools/releases/tag/studio-v1-durable-5dbf42a51c83) |
| Source SHA (merged main) | `5dbf42a51c834c129530be2bc86feaea207be99e` |
| buildId | `f654796859ea2b6ba3a6e10099c1a7f740e0ec486c33ad81eba19e0a371bae57` |
| cacheId | `b3fe1d4dcc8b01a68ec34480d2cdf92480c2be562f5b2fa288d41d1f724a57ea` |
| Asset count | 157 (v6: 156; `studio/web/listen-paste.mjs` is new) |
| Runtime ZIP | `8666b6b8d561ce81b9db850f6ee8bb3404aa6328a195bc6992954d2bb133a7f1`, 4,271,954 bytes |
| Trust ZIP | `28bdb56bf792105fe310737d6bb3e93c971fe3c46e64d9d40a45294a147e5a5b`, 18,984 bytes (the same bytes as v4–v6) |
| Rendered function file | `9afd162e451c99e00cbcbb790a71e18d6ea20d6e141c8a080154b319e0dfdbdb`, 41,250 bytes (embedded self-hash `5b746a472c3c…`) |
| Canonical | `2026-09-23-v3` / `PUBLISHED` / `2026-09-23-v3-manifest1` / `ff1a9df054f5ca1ae42571067fc95feb274755ef`, `machine_delivery_schema: mabinogi-mobile-mml-studio/machine-delivery@2` |
| Manifest commit | `44f3f0082cf5c30328edf1c251398b844488ad0a` |

## How it was produced

1. **Pin.** The buildId was computed twice from main `5dbf42a51c83`, once with the local dependencies and once after a fresh `npm install --ignore-scripts --package-lock=false`. Both gave the same buildId, `verify-studio-artifact.mjs` passed, and `package_release.py` produced byte-identical ZIPs and lock from both builds.
2. **Build and publish.** [Release run 36132530795](https://github.com/a91453/mml-tools/actions/runs/36132530795) was dispatched from the pin branch while `origin/main` was exactly the source SHA.
3. **Mirror to the bucket.** [Mirror run 36132589479](https://github.com/a91453/mml-tools/actions/runs/36132589479) uploaded both ZIPs to the lock's object keys and read each back.
4. **Local proof under Bun** (before deploying).
   - `bootstrap.test.mjs` (2/2) and `publish-release.test.mjs` (7/7) passed against these assets.
   - The rendered function (rendered twice, identical) started offline from a cache seeded with the runtime ZIP: `RENDERED_SOURCE_VERIFIED` → `TRUSTED_SOURCE_VERIFIED` → `ARTIFACT_VERIFIED` → `VALIDATED_CACHE_STARTUP` → `READY_MARKER_WRITTEN_AFTER_VERIFICATION` → `SERVER_STARTED`, with `/health` naming this buildId and pages sent with `frame-ancestors 'none'` and `X-Frame-Options: DENY`.
   - A copy with one character changed exited with `RENDERED_SOURCE_MISMATCH`.
5. **Deploy.** The function source was staged and read back byte-identical (SHA-256 `9afd162e…`); the staged patch changed only the source code and was not destructive. The owner committed it from the Dashboard on 2026-09-25 12:17 UTC. The deployment reached `SUCCESS`; the v6 deployment became `REMOVED`.

## Startup log, in order

`RENDERED_SOURCE_VERIFIED` (`5b746a472c3c…`) → `TRUSTED_SOURCE_VERIFIED`
(trust ZIP `28bdb56bf792…`, source `5dbf42a51c83`) → `DURABLE_DOWNLOAD_STARTED`
→ `DURABLE_ZIP_VERIFIED` (4,271,954 bytes) → `ARTIFACT_VERIFIED` (157 assets,
Canonical as above) → `ARTIFACT_ATOMICALLY_COMMITTED`
(`/studio-cache/durable-v1/f6547968…`) → `READY_MARKER_WRITTEN_AFTER_VERIFICATION`
→ `SERVER_STARTED` (`bootstrap: durable-source`); dependency discovery `{}`
("No dependencies found, skipping bun install").

## Not verified from here

The session container cannot reach `*.up.railway.app`, so `verify_deployed.py`
was not run against the permanent URL. A real-browser check by the owner is
still open (pasting MML into listening, the Workshop, a damaged sound bank being
refused, the page refusing to load inside a frame).

## Rollback

- The v6 cache `durable-v1/9d0991bd…` stays on the volume, so rollback starts from cache.
- **R1:** render the function from the ops files at merged main `42f38f695f82` (the v6 lock; `build_function.py`) and replace the function source with it. Expect `VALIDATED_CACHE_STARTUP` with `refetch: false`.
- **Alternatively:** roll back to the v6 deployment in the Dashboard's deployment history.
- Never delete cache namespaces, bucket objects, Releases or tags.
