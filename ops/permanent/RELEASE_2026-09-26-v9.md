# Durable release 2026-09-26 (v9) — main 718c8452dd4f

> Railway resource IDs are kept out of this public repository. Read them from the Railway dashboard.

Status: **pinned; not yet published, mirrored or deployed.** This record is
updated as each step below completes.

## Re-pinned from 44c282f1f32a

v9 was first pinned to main `44c282f1f32a` (#119–#121). That pin was merged
into main before its release run, so `origin/main` no longer matched and the
workflow could not publish it; it was never published, mirrored or deployed.
Its ZIPs stay under `assets/` as history. v9 is re-pinned here to merged main
`718c8452dd4f`, which carries everything the first pin did and, after it:

- #123, #127, #129 (legacy-site migration review), #124 (Studio main page
  dark mode), #125 (Studio source intake reads 3MLE `.mml`／`.mmi`), #126 (the
  Workshop's zh-Hant help pages), #128 (misleading Workshop hints), #131 (the
  Workshop in zh-Hant only);
- #132: a failed Workshop autosave is shown and retryable, and applying a new
  release or reloading a stale tab flushes first and is refused when the
  flush fails;
- #133: the preview server serves on Windows (path containment used `/`), and
  opening a library song clears the previous Studio source label and return
  target.

The Published Canonical is unchanged from v3–v8.

## Identity

| Field | Value |
| --- | --- |
| Release/tag | `studio-v1-durable-718c8452dd4f` (after the release run) |
| Source SHA (merged main) | `718c8452dd4fd717346045bd8e954c6be05289b9` |
| buildId | `e5b22614b0ead493e301d4e0d7bef86ac00fcda0c0fdbe82fa19d7ae4872fdd8` |
| cacheId | `d451746f5e98dee09598deac3fed97aa7c4b741b51c8103e4786785efb5a2450` |
| Asset count | 170 (v8: 158) |
| Runtime ZIP | `b8fd121eb6c72f51e335a0b6f061a379c7f535a29bf861432b83b21a8479350e`, 4,353,011 bytes |
| Trust ZIP | `28bdb56bf792105fe310737d6bb3e93c971fe3c46e64d9d40a45294a147e5a5b`, 18,984 bytes (the same bytes as v4–v8: the four trusted files did not change) |
| Rendered function file | `1c05256427755a0672ba8c17501bcef2550cbeaaa7d5b6c9d46392324038ebb8`, 44,512 bytes |
| Canonical | `2026-09-23-v3` / `PUBLISHED` / `2026-09-23-v3-manifest1` / `ff1a9df054f5ca1ae42571067fc95feb274755ef`, `machine_delivery_schema: mabinogi-mobile-mml-studio/machine-delivery@2` |
| Manifest commit | `44f3f0082cf5c30328edf1c251398b844488ad0a` |

## How it was produced

1. **Pin.** The buildId was computed twice from main `718c8452dd4f`, each in
   its own clean worktree: once after a fresh `npm install --ignore-scripts
   --package-lock=false`, as the release workflow runs it, and once with the
   local dependencies. Both gave the same buildId, `verify-studio-artifact.mjs`
   reported `ARTIFACT_VERIFIED` for both, and `package_release.py` produced
   byte-identical ZIPs and lock from both builds.
2. **Local proof under Bun 1.3** (before publishing).
   - `bootstrap.test.mjs` (5/5, with `RELEASE_TEST_ASSETS=ops/permanent/assets`
     as CI runs it) and `publish-release.test.mjs` (7/7) passed.
   - The rendered function (rendered twice, identical) started offline from a
     cache seeded with the runtime ZIP: `RENDERED_SOURCE_VERIFIED` →
     `TRUSTED_SOURCE_VERIFIED` → `ARTIFACT_VERIFIED` → `VALIDATED_CACHE_STARTUP`
     → `READY_MARKER_WRITTEN_AFTER_VERIFICATION` → `SERVER_STARTED`. `/health`
     and `/build.json` named this buildId and source SHA, pages carried
     `frame-ancestors 'none'` and `X-Frame-Options: DENY`, the served
     `studio/web/workshop/ui.mjs` carried #133's fix, and the game-style bank
     answered 503 with the bucket unreachable.
   - A copy with one character changed exited with `RENDERED_SOURCE_MISMATCH`.

## Still to do, in order

1. **Publish.** Dispatch `Studio durable release` from the pin branch **while
   `origin/main` is exactly `718c8452dd4f`**: the workflow refuses anything
   else. No pull request may merge into main before this run, the pin PR
   included; if main moves, v9 must be re-pinned to the new main.
2. **Mirror.** Dispatch `Studio durable release mirror` from the same branch.
   It needs the five `RELEASE_S3_*` repository secrets for the transfer only;
   delete them afterwards.
3. **Merge** the pin PR.
4. **Deploy.** Replace the `studio-web-permanent` function source with the
   rendered file above (`python ops/permanent/build_function.py
   ops/permanent/assets <out.ts>`, SHA-256 `1c052564…`) and create a new
   deployment from current config (README steps 4–9). Expect a cold download:
   `DURABLE_DOWNLOAD_STARTED` → `DURABLE_ZIP_VERIFIED` → `ARTIFACT_VERIFIED`
   (170 assets) → `ARTIFACT_ATOMICALLY_COMMITTED` →
   `READY_MARKER_WRITTEN_AFTER_VERIFICATION` → `SERVER_STARTED`. Never through
   Railway Agent (`docs/RAILWAY_AGENT_POLICY.md`).

## Rollback

- The previous release's cache stays on the volume, so rollback starts from cache.
- Render the function from the ops files of the previous lock
  (`build_function.py`) and replace the function source with it, or roll back
  to the previous deployment in the Dashboard's deployment history.
