# Permanent Studio v9 發布準備

狀態：準備中，尚未發布、尚未部署。這是實作／操作紀錄，不是 Canonical 規則。

- runtime 來源：已合併 main `44c282f1f32a9ea036c57dfd8239c7509aad0b3e`（含 #119–#121）。
- Published Canonical：`2026-09-23-v3`。
- rules_snapshot_sha：`ff1a9df054f5ca1ae42571067fc95feb274755ef`。
- Manifest commit：`44f3f0082cf5c30328edf1c251398b844488ad0a`。
- 工作分支：`ops/permanent-v9-20260926`，禁止自行合併。

預定完成固定來源的 deterministic runtime/trust 套件、release lock、workflow pins、bootstrap 驗證與擁有者的 Dashboard 操作步驟。

初次本機驗證：`npm test` 已執行；兩項既有測試受目前容器檔案系統行為影響（chown 回 EINVAL；remove-repository 的 ENOTDIR 行為不同），不能宣稱全綠。`build:studio-web` 與 artifact verifier 成功；`test:studio-web` 因 Chromium／WebKit 未安裝而 NOT_RUN，瀏覽器下載失敗。最終以 PR 的 Studio CI、Studio service CI 實跑結果決定是否可合併。
